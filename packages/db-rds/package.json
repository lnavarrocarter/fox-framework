{
  "name": "@foxframework/db-rds",
  "version": "1.4.4",
  "description": "AWS RDS provider for Fox Framework — PostgreSQL, MySQL, Aurora with SSL and entity auto-creation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist/**/*", "README.md", "LICENSE", "package.json"],
  "publishConfig": { "access": "public" },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "keywords": ["fox-framework", "aws", "rds", "postgresql", "mysql", "aurora", "typescript"],
  "peerDependencies": {
    "@foxframework/core": ">=1.0.0",
    "@foxframework/db-postgres": ">=1.0.0",
    "@foxframework/db-mysql": ">=1.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0",
    "@foxframework/core": "*",
    "@foxframework/db-postgres": "*",
    "@foxframework/db-mysql": "*"
  }
}
