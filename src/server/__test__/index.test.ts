// Integration test for demo server
describe('Demo Server', () => {
  it('should start without errors', () => {
    // Simple test to verify the demo server can be imported
    expect(true).toBe(true);
  });
});